import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    image: {
        resizeMode: 'contain'
    },
    container: {
        alignItems: 'center',
        margin: 5,
    },
    rating: {
        alignItems: 'center',
        marginBottom: 2,
    },
    buttonStyle: {
        borderRadius: 0, 
        marginLeft: 0, 
        marginRight: 0, 
        marginBottom: 0
    },
});

export default style;